<UserControl x:Class="TechReportToDB.Views.Pages.PadMapPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TechReportToDB.Views.Pages"
             mc:Ignorable="d" 
             xmlns:map="clr-namespace:TechReportToDB.Views.CustomControls"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        
        <GridSplitter Grid.Row="1" 
              Grid.Column="1" 
              Grid.RowSpan="4" 
              Width="5"
              VerticalAlignment="Stretch" 
              HorizontalAlignment="Center" />

        <!--Search-->
        <Grid Grid.ColumnSpan="11">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Content="Назад"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="5"
                        Command="{Binding GoToDailyReportToDbPageCommand}"/>
                
                <TextBlock Text="Поиск:"
                    Margin="5"
                    FontSize="14"
                    VerticalAlignment="Center"/>
            </StackPanel>

            <TextBox  Grid.Column="1"  
                Margin="5"
                VerticalContentAlignment="Center"
                FontSize="12"
                Text="{Binding FilterJob, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>


        <Grid Grid.Row="2"
              Grid.RowSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Работы"
                       HorizontalAlignment="Center"
                       Margin="5"/>

            <ListBox Grid.Row="2"
              ItemsSource="{Binding JobList}" VerticalContentAlignment="Stretch"
              ScrollViewer.VerticalScrollBarVisibility="Auto"
              SelectedItem="{Binding SelectedJob, Mode=TwoWay}">
                <ListBox.ItemTemplate>

                    <DataTemplate>

                        <StackPanel Margin="0,3">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Field}"
                                    FontWeight="Bold"/>
                                <TextBlock Text=" к. "
                                     FontWeight="Bold"/>
                                <TextBlock Text="{Binding Pad}"
                                     FontWeight="Bold"/>
                                <TextBlock Text=" скв. "
                                     FontWeight="Bold"/>
                                <TextBlock Text="{Binding Well}"
                                    FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Полевая партия №"/>
                                <TextBlock Text="{Binding FieldTeam}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Тел.  "/>
                                <TextBlock Text="{Binding Phone}"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Grid Grid.Column="2" Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="5" VerticalAlignment="Center">
               
            </StackPanel>
            <map:YandexMap x:Name="YandexMapControl" Grid.Row="1"/>
        </Grid>
    </Grid>
</UserControl>
